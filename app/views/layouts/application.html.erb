<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark p-4">
      <%= link_to "Private Events", events_path, class: "navbar-brand" %>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <%= link_to "Events", events_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Party People", profiles_path, class: "nav-link" %>
          </li>
        </ul>
        <div class="nav navbar-nav ms-auto">
          <% if current_user %>
            <div class="navbar-text">
              <p class="me-2">Logged in as <%= link_to "#{current_user.name}", profile_path(current_user) %></p>
            </div>
            <div class="nav-item">
              <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-primary sign-out-btn" %>
            </div>
          <% else %>
            <div class="nav-item">
              <%= link_to "Log In", new_user_session_path, class: "nav-link mr-4" %>
            </div>
            <div class="nav-item">
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary sign-up-btn" %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <p class="notice alert alert-success position-absolute w-100"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-warning position-absolute w-100" role="alert"><%= alert %></p>
    <% end %>

    <main class="row d-flex justify-content-center p-4 mt-5">
      <%= yield %>
    </main>
  </body>
</html>
